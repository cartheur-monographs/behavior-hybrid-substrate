A list of human things to check

15.07.2026

Committed as `7b0df50f` with message `Record 1737 block-context execution findings`.

What you should investigate next is the block-context difference, because that is now the real blocker.

1. In the live `arrayForth` session, check whether `1737 LOAD` succeeds only because it is executed as a stored block, while the same text fails when typed manually.
2. Investigate what `clc` depends on in that context. The visible text matches the stored block, but prompt entry stops at `clc ?`, so either `clc` is only available in a different compile state, or block loading sets something up that prompt entry does not.
3. Try to clone `1737` into a fresh working block inside the live environment, then make only one tiny edit to `: go ...`. That will tell us whether mutation works when we preserve the original block execution context.
4. If possible, inspect which editor/block commands are the safest for duplicating and editing a block in place. That is likely the cleanest route to the first custom SOFTSIM experiment.

The key question to answer is: “What does block execution provide that prompt re-entry does not?” Once we know that, the path to a custom one-node variant should become much clearer.

Operator check to add at the end of the human investigation:

5. In the next live session, do not begin with more prompt-entry variants. First confirm `SOFTSIM LOAD`, `1737 LOAD`, and `10 SUPER` again, then inspect block `1737`, duplicate it into a fresh working block, verify the duplicate text, and change only one tiny part of `: go`. Load that duplicated block through the same stored-block path and record whether it steps successfully or exactly where it fails.
